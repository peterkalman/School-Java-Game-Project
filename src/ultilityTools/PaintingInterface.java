package ultilityTools;

import java.awt.Graphics;
/**
 * every object that wants to be displayed on map must implement this interface
 * @author Peter
 *
 */
public interface PaintingInterface {
	void myPaintComponent(Graphics g);
}
