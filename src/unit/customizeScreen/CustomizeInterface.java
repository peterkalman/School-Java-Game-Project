package unit.customizeScreen;
/**
 * CustomizeInterface is an interface implemented by PlayerG in order to notify when a Clothing button is clicked
 * @author Peter
 *
 */
public interface CustomizeInterface {
	void CustomizeButtonClicked(String name,boolean isNext);
}
